version: "3.7"

services:
    backend:
        build:
            context: .
        ports:
            - "80:80"
        volumes:
            - ./backend:/backend
        environment:
            - DEBUG=1
            - ALLOWED_HOSTS=127.0.0.1,localhost,34.125.175.117,10.182.0.2
